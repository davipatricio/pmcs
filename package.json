{
  "name": "@pmcs/pmcs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "prepare": "is-ci || husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "husky": "^9.0.10",
    "is-ci": "^3.0.1",
    "turbo": "^1.12.3",
    "typescript": "^5.3.3"
  }
}
